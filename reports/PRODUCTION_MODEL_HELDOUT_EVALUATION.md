# Production model held-out evaluation

**NOT RUN.** No frozen target-compatible neuronal instance-label test set is
available. No checkpoint has been selected, and full-volume processing remains
`BLOCKED_MODEL_VALIDATION`.

When a test set is frozen, this report must record its immutable source and
mask hashes, evaluated voxels, ground-truth/predicted object counts, merge and
split counts, missed/false objects, VI split/merge/total, visual error sample
selection seed, and limitations. It must not be replaced by training loss or
qualitative screenshots.

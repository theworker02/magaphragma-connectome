# Synthetic test data only

Anything in this directory must use the `SYN-` identifier namespace and be labelled `SYNTHETIC TEST DATA — NOT BIOLOGICAL RECONSTRUCTION` in any rendered interface. The release verifier rejects this namespace from `biological/` collections.
